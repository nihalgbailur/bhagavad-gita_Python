# Random bhagavad-gita Quotes Generator

This Python script generates random quotes from a list. Each quote is accompanied by its source.

## Usage

Run the script to get a random quote:

```bash
python random_quotes.py
